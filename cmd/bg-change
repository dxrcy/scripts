#!/bin/sh
#~~~ shuffle backgrounds every so often

dir="$HOME/pics/wallpapers"
delay=300 # 5 minutes

while true; do
    feh --randomize --bg-scale "$dir/"*'.jpg'

    if [ ! "$1" = '--forever' ]; then
        exit
    fi

    sleep "$delay"
done

